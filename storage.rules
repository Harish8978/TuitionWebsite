rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && (request.auth.token.email == "workwithharishp@gmail.com" || request.auth.token.email == "prathipa014@gmail.com" || request.auth.token.email == "aaswindark@gmail.com");
    }
  }
}
